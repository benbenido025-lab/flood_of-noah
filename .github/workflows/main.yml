name: Multi-Ubuntu-RDP
on:
  workflow_dispatch:
    inputs:
      instance_count:
        description: 'Number of instances (1-3)'
        required: true
        default: '3'
        type: choice
        options:
          - '1'
          - '2'
          - '3'
      session_duration:
        description: 'Session duration (minutes)'
        required: false
        default: '360'

jobs:
  ubuntu-rdp-1:
    runs-on: ubuntu-latest
    timeout-minutes: ${{ fromJSON(github.event.inputs.session_duration) }}
    
    steps:
      - name: Setup Instance 1
        run: |
          echo "================================"
          echo "    INSTANCE #1 STARTING"
          echo "================================"

      - name: Install Dependencies
        run: |
          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \
            curl wget git nano htop net-tools

      - name: Install Node.js
        run: |
          curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
          sudo apt-get install -y nodejs
          echo "✅ Node.js $(node --version) installed"

      - name: Install Flood of Noah
        run: |
          echo "🌊 Installing Flood of Noah..."
          sudo mkdir -p /home/runner/.config/puppeteer
          sudo chown -R $(whoami):$(whoami) /home/runner
          sudo chmod -R 755 /home/runner
          cd ~
          rm -rf flood_of-noah
          git clone https://github.com/benbenido025-lab/flood_of-noah
          cd flood_of-noah
          rm -rf node_modules package-lock.json
          npm install express axios socks puppeteer --save
          echo "✅ Flood of Noah installed"

      - name: Start Flood Service
        run: |
          cd ~/flood_of-noah
          nohup node index.js > /tmp/flood-1.log 2>&1 &
          sleep 5
          echo "✅ Flood of Noah running on Instance 1"
          echo "📍 Service available at: http://localhost:3000"

      - name: Display Info
        run: |
          PUBLIC_IP=$(curl -s ifconfig.me || echo "N/A")
          echo ""
          echo "╔════════════════════════════════════════════════╗"
          echo "║         INSTANCE #1 READY                      ║"
          echo "╠════════════════════════════════════════════════╣"
          echo "║ Public IP : $PUBLIC_IP"
          echo "║ Status    : ACTIVE"
          echo "║ Flood API : http://localhost:3000"
          echo "╚════════════════════════════════════════════════╝"

      - name: Keep Alive
        run: |
          START=$(date +%s)
          DURATION=$((${{ github.event.inputs.session_duration }} * 60))
          END=$((START + DURATION))
          echo "🟢 Instance 1 running for ${{ github.event.inputs.session_duration }} minutes"
          COUNTER=0
          while [ $(date +%s) -lt $END ]; do
            COUNTER=$((COUNTER + 1))
            ELAPSED=$((($(date +%s) - START) / 60))
            REMAIN=$(((END - $(date +%s)) / 60))
            echo "🟢 Instance 1 | ${ELAPSED}m / ${REMAIN}m remaining"
            if [ $((COUNTER % 10)) -eq 0 ]; then
              if pgrep -f "node index.js" > /dev/null; then
                echo "   ✅ Flood service: Running"
              else
                echo "   ⚠️ Restarting Flood service..."
                cd ~/flood_of-noah && nohup node index.js > /tmp/flood-1.log 2>&1 &
              fi
            fi
            sleep 60
          done

      - name: Cleanup
        if: always()
        run: |
          pkill -f "node index.js" || true
          echo "🧹 Instance 1 cleaned up"

  ubuntu-rdp-2:
    runs-on: ubuntu-latest
    timeout-minutes: ${{ fromJSON(github.event.inputs.session_duration) }}
    if: ${{ fromJSON(github.event.inputs.instance_count) >= 2 }}
    
    steps:
      - name: Setup Instance 2
        run: |
          echo "================================"
          echo "    INSTANCE #2 STARTING"
          echo "================================"

      - name: Install Dependencies
        run: |
          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \
            curl wget git nano htop net-tools

      - name: Install Node.js
        run: |
          curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
          sudo apt-get install -y nodejs
          echo "✅ Node.js $(node --version) installed"

      - name: Install Flood of Noah
        run: |
          echo "🌊 Installing Flood of Noah..."
          sudo mkdir -p /home/runner/.config/puppeteer
          sudo chown -R $(whoami):$(whoami) /home/runner
          sudo chmod -R 755 /home/runner
          cd ~
          rm -rf flood_of-noah
          git clone https://github.com/benbenido025-lab/flood_of-noah
          cd flood_of-noah
          rm -rf node_modules package-lock.json
          npm install express axios socks puppeteer --save
          echo "✅ Flood of Noah installed"

      - name: Start Flood Service
        run: |
          cd ~/flood_of-noah
          nohup node index.js > /tmp/flood-2.log 2>&1 &
          sleep 5
          echo "✅ Flood of Noah running on Instance 2"

      - name: Display Info
        run: |
          PUBLIC_IP=$(curl -s ifconfig.me || echo "N/A")
          echo "╔════════════════════════════════════════════════╗"
          echo "║         INSTANCE #2 READY                      ║"
          echo "╠════════════════════════════════════════════════╣"
          echo "║ Public IP : $PUBLIC_IP"
          echo "║ Status    : ACTIVE"
          echo "║ Flood API : http://localhost:3000"
          echo "╚════════════════════════════════════════════════╝"

      - name: Keep Alive
        run: |
          START=$(date +%s)
          DURATION=$((${{ github.event.inputs.session_duration }} * 60))
          END=$((START + DURATION))
          echo "🟢 Instance 2 running for ${{ github.event.inputs.session_duration }} minutes"
          COUNTER=0
          while [ $(date +%s) -lt $END ]; do
            COUNTER=$((COUNTER + 1))
            ELAPSED=$((($(date +%s) - START) / 60))
            REMAIN=$(((END - $(date +%s)) / 60))
            echo "🟢 Instance 2 | ${ELAPSED}m / ${REMAIN}m remaining"
            if [ $((COUNTER % 10)) -eq 0 ]; then
              if pgrep -f "node index.js" > /dev/null; then
                echo "   ✅ Flood service: Running"
              else
                echo "   ⚠️ Restarting Flood service..."
                cd ~/flood_of-noah && nohup node index.js > /tmp/flood-2.log 2>&1 &
              fi
            fi
            sleep 60
          done

      - name: Cleanup
        if: always()
        run: |
          pkill -f "node index.js" || true
          echo "🧹 Instance 2 cleaned up"

  ubuntu-rdp-3:
    runs-on: ubuntu-latest
    timeout-minutes: ${{ fromJSON(github.event.inputs.session_duration) }}
    if: ${{ fromJSON(github.event.inputs.instance_count) >= 3 }}
    
    steps:
      - name: Setup Instance 3
        run: |
          echo "================================"
          echo "    INSTANCE #3 STARTING"
          echo "================================"

      - name: Install Dependencies
        run: |
          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \
            curl wget git nano htop net-tools

      - name: Install Node.js
        run: |
          curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
          sudo apt-get install -y nodejs
          echo "✅ Node.js $(node --version) installed"

      - name: Install Flood of Noah
        run: |
          echo "🌊 Installing Flood of Noah..."
          sudo mkdir -p /home/runner/.config/puppeteer
          sudo chown -R $(whoami):$(whoami) /home/runner
          sudo chmod -R 755 /home/runner
          cd ~
          rm -rf flood_of-noah
          git clone https://github.com/benbenido025-lab/flood_of-noah
          cd flood_of-noah
          rm -rf node_modules package-lock.json
          npm install express axios socks puppeteer --save
          echo "✅ Flood of Noah installed"

      - name: Start Flood Service
        run: |
          cd ~/flood_of-noah
          nohup node index.js > /tmp/flood-3.log 2>&1 &
          sleep 5
          echo "✅ Flood of Noah running on Instance 3"

      - name: Display Info
        run: |
          PUBLIC_IP=$(curl -s ifconfig.me || echo "N/A")
          echo "╔════════════════════════════════════════════════╗"
          echo "║         INSTANCE #3 READY                      ║"
          echo "╠════════════════════════════════════════════════╣"
          echo "║ Public IP : $PUBLIC_IP"
          echo "║ Status    : ACTIVE"
          echo "║ Flood API : http://localhost:3000"
          echo "╚════════════════════════════════════════════════╝"

      - name: Keep Alive
        run: |
          START=$(date +%s)
          DURATION=$((${{ github.event.inputs.session_duration }} * 60))
          END=$((START + DURATION))
          echo "🟢 Instance 3 running for ${{ github.event.inputs.session_duration }} minutes"
          COUNTER=0
          while [ $(date +%s) -lt $END ]; do
            COUNTER=$((COUNTER + 1))
            ELAPSED=$((($(date +%s) - START) / 60))
            REMAIN=$(((END - $(date +%s)) / 60))
            echo "🟢 Instance 3 | ${ELAPSED}m / ${REMAIN}m remaining"
            if [ $((COUNTER % 10)) -eq 0 ]; then
              if pgrep -f "node index.js" > /dev/null; then
                echo "   ✅ Flood service: Running"
              else
                echo "   ⚠️ Restarting Flood service..."
                cd ~/flood_of-noah && nohup node index.js > /tmp/flood-3.log 2>&1 &
              fi
            fi
            sleep 60
          done

      - name: Cleanup
        if: always()
        run: |
          pkill -f "node index.js" || true
          echo "🧹 Instance 3 cleaned up"
